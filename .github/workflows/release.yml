name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-arm64:
    runs-on: ubuntu-latest
    steps:
      - name: Build Paused
        run: |
          echo "Binary bundling and .deb packaging are temporarily paused."
          echo "Please use the install.sh script for direct Python-based installation."
          exit 0
